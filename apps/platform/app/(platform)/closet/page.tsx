import React from 'react';

import ClosetView from './_layers/closet-view';

const ClosetPage = () => {
  return (
    <main className="flex min-h-screen w-full flex-col items-center">
      <ClosetView />
    </main>
  );
};

export default ClosetPage;
