import { Card, CardHeader, CardTitle } from '@openhome-os/ui/card';

const HomeLocationsCard = () => {
  return (
    <Card variant={'ring'} className="w-full">
      <CardHeader>
        <CardTitle>Locations</CardTitle>
      </CardHeader>
    </Card>
  );
};

export default HomeLocationsCard;
